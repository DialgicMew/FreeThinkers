class History {
  String user;
  String url;
  History({
    this.user,
    this.url,
  });
//   History.fromSnapshot(DocumentSnapshot snapshot)
//       : url = snapshot['url'],
//         user = snapshot['user'];
}
